"use client";
import {useState} from "react"; import {BarChart3,Loader2,RefreshCw} from "lucide-react"; import {Button} from "@/components/ui/button"; import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card"; import {analyticsService} from "@/services/analytics-service"; import type {AICFOAnswer} from "@/types/analytics";
export default function BenchmarkingPage(){const [data,setData]=useState<AICFOAnswer|null>(null),[loading,setLoading]=useState(false);async function run(){setLoading(true);try{setData(await analyticsService.getIndustryBenchmark())}finally{setLoading(false)}}return <div className="mx-auto max-w-6xl space-y-6"><div><p className="text-sm text-muted-foreground">External intelligence</p><h1 className="text-3xl font-semibold">Industry benchmarking</h1><p className="mt-2 text-muted-foreground">Compare your internal performance with credible current industry and economic evidence. FinCruiz will say when a reliable benchmark is unavailable rather than inventing one.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><BarChart3 className="size-5"/>Automated benchmark</CardTitle><CardDescription>Uses your company profile and aggregated finance results; raw GL rows are not sent for web research.</CardDescription></CardHeader><CardContent><Button onClick={()=>void run()} disabled={loading}>{loading?<Loader2 className="size-4 animate-spin"/>:<RefreshCw className="size-4"/>}{data?"Refresh benchmark":"Generate benchmark"}</Button>{data?<><div className="mt-6 whitespace-pre-wrap text-sm leading-7">{data.answer}</div>{data.sources?.length?<div className="mt-6 space-y-2"><p className="font-medium">Sources</p>{data.sources.map(s=><a key={s.url} href={s.url} target="_blank" rel="noreferrer" className="block text-sm underline underline-offset-4">{s.title}</a>)}</div>:null}</>:null}</CardContent></Card></div>}

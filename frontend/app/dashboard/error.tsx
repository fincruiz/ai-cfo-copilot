"use client";
import { AlertTriangle, RefreshCcw } from "lucide-react";
import { Button } from "@/components/ui/button";
export default function DashboardError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="mx-auto flex min-h-[60vh] max-w-2xl items-center px-5"><div className="w-full rounded-3xl border bg-card p-8 text-center"><div className="mx-auto flex size-12 items-center justify-center rounded-2xl bg-amber-100 text-amber-800"><AlertTriangle className="size-6"/></div><h2 className="mt-5 text-2xl font-bold">This section could not load.</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Your workspace is still safe. Retry the request; if it continues, check that the backend is running and the latest database migrations have been applied.</p><p className="mt-3 rounded-xl bg-muted p-3 text-xs text-muted-foreground">{error.message}</p><Button className="mt-5" onClick={reset}><RefreshCcw className="size-4"/>Try again</Button></div></div>}

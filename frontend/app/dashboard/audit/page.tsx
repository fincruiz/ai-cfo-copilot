"use client";
import {useEffect,useState} from "react"; import {History,Loader2} from "lucide-react"; import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card"; import {auditService,type AuditEvent} from "@/services/audit-service";
export default function AuditPage(){const [rows,setRows]=useState<AuditEvent[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{auditService.list().then(setRows).finally(()=>setLoading(false))},[]);return <div className="mx-auto max-w-6xl space-y-6"><div><p className="text-sm text-muted-foreground">Security & governance</p><h1 className="text-3xl font-semibold">Audit trail</h1><p className="mt-2 text-muted-foreground">A privacy-safe history of important workspace actions. Deleted financial rows are never copied into this log.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><History className="size-5"/>Recent activity</CardTitle><CardDescription>Uploads, resets and other important changes.</CardDescription></CardHeader><CardContent>{loading?<div className="flex gap-2"><Loader2 className="size-4 animate-spin"/>Loading…</div>:<div className="space-y-2">{rows.length?rows.map(r=><div key={r.id} className="flex flex-col gap-1 rounded-xl border p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">{r.summary}</p><p className="text-xs text-muted-foreground">{r.module.replaceAll("_"," ")} · {r.action}</p></div><time className="text-sm text-muted-foreground">{new Date(r.created_at).toLocaleString()}</time></div>):<p className="text-sm text-muted-foreground">No audited activity yet.</p>}</div>}</CardContent></Card></div>}

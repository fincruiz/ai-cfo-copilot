# Legacy finance reference

These files are retained only to verify formula and behavioural parity during migration.
Production code must not import this folder. The adopted implementation lives under
`app/domain/finance`, `app/services/finance`, `app/repositories/finance`, and `app/api/v1/finance`.
